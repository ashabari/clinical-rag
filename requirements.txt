# Direct dependencies only, derived from `import` statements across
# src/ and eval/. Transitive dependencies (numpy, torch, transformers,
# huggingface_hub, etc.) are resolved automatically by pip.
#
# The spaCy NER model is installed separately (see README):
#   python -m spacy download en_core_web_trf

anthropic==0.109.1
chromadb==1.5.9
FlashRank==0.2.10
matplotlib==3.10.9
pandas==3.0.3
python-dotenv==1.2.2
rank-bm25==0.2.2
sentence-transformers==5.5.1
spacy==3.8.14
tqdm==4.68.2
